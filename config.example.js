// Sao chép file này thành config.js rồi điền khoá API thật vào.
// KHÔNG đưa config.js lên Git — file này đã được liệt kê trong .gitignore.
//
// Lấy khoá tại: https://platform.openai.com/api-keys

const CONFIG = {
  OPENAI_API_KEY: 'sk-REPLACE_WITH_YOUR_KEY',
};
